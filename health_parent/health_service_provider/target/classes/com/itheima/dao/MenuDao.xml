<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.itheima.dao.MenuDao">

    <resultMap id="baseResultMap" type="com.itheima.pojo.Menu">
        <id column="id" property="id"/>
        <result column="member_id" property="memberId"/>
        <result column="orderDate" property="orderDate"/>
        <result column="orderType" property="orderType"/>
        <result column="orderStatus" property="orderStatus"/>
        <result column="setmeal_id" property="setmealId"/>
    </resultMap>

    <resultMap id="findMenuResultMap" type="com.itheima.pojo.Menu" extends="baseResultMap">
        <collection property="children"
                    ofType="com.itheima.pojo.Menu"
                    column="id"
                    select="com.itheima.dao.MenuDao.findChildrenMenu"/>
    </resultMap>

    <select id="findChildrenMenu" parameterType="java.lang.Integer" resultMap="findMenuResultMap">
        SELECT
             *
        FROM
             t_menu
        WHERE
             parentMenuId = #{menuId}
    </select>


    <select id="findByRoleId" parameterType="java.lang.Integer" resultMap="findMenuResultMap">
        SELECT
            m.*
        FROM
            t_menu AS m,
            t_role_menu AS rm
        WHERE
            m.id = rm.menu_id
        AND
            m.parentMenuId is null
        AND
            rm.role_id = #{roleId}
    </select>



<!--    =========================递归方法分割线=========================             -->
    <select id="findParentMenuByRoleId" parameterType="java.lang.Integer" resultType="com.itheima.pojo.Menu">
        SELECT
            m.*
        FROM
            t_menu AS m,
            t_role_menu AS rm
        WHERE
            m.id = rm.menu_id
        AND
            m.parentMenuId is null
        AND
            rm.role_id = #{roleId}
    </select>


    <select id="findChildrenMenuById" parameterType="java.lang.Integer" resultType="com.itheima.pojo.Menu">
        SELECT
             *
        FROM
             t_menu
        WHERE
             parentMenuId = #{menuId}
    </select>

</mapper>